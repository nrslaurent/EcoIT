{% extends 'base.html.twig' %}
{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container-fluid container-lg">
  <div class="row">
    <div class="d-flex flex-column">
      <p class="my-2"><span class="fs-5 fw-bold">EcoIT</span> est une plateforme d’éducation éco-responsable dont les cours sont dispensés par des experts en accessibilité et en éco-conception web.</p>
      <p class="fst-italic align-bottom my-2">Nos dernières formations publiées:</p>
    </div>
  </div>
  <div class="row d-flex justify-content-evenly align-items-center overflow-auto">
  {% for course in courses %}
    <div class="col-lg-3">
      <div class="card mx-4 my-5">
        <div class="courseCard">
          <img src="{{ asset('uploads/images/'  ~ course.picture) }}" class="card-img-top mb-2" alt="course_picture">
          <div class="card-body col courseCardText d-flex flex-column justify-content-between">
            <h5 class="card-title text-center">{{course.title}}</h5>
            <p class="card-text overflow-auto">{{course.description}}.</p>
            <a href="{{ path('app_course_inprogress',{"id":course.id}) }}" class="btn btn-french-lilac align-self-end">Accéder</a>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}
  </div>
</div>

{% endblock %}
